enclave {
    from "openenclave/edl/syscall.edl" import *;
    from "platform.edl" import *;

    include "../shared.h"

    trusted {
        public int setup([out] endorser_id_t* endorser_id);
        public int new_ledger([in] handle_t* handle, [out] signature_t* signature);
        public int read_latest([in] handle_t* handle, [in] nonce_t* nonce, [out] digest_t* tail, [out] signature_t* signature);
        public int append([in] handle_t* handle, [in] digest_t* block_hash, [out] signature_t* signature);
        public int get_public_key([out] endorser_id_t* endorser_id);
        public void terminate();
        public int verify_append([in] endorser_id_t* endorser_id, [in] handle_t* handle, [in] digest_t* block_hash, [out] signature_t* signature);
};

    //untrusted {
    //    no untrusted functions in the endorser 
    //};
};
